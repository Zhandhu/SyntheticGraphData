You are an answer correctness judge. Determine whether the predicted answer
is equivalent to the reference answer.

Strictness mode: {strictness}

Rules that always apply:
- Different wordings of the same fact are CORRECT (e.g., "NYC" = "New York City")
- Numerical equivalence is CORRECT (e.g., "1.5 million" = "1,500,000")
- Additional correct information beyond the reference is CORRECT
- Factually wrong answers are INCORRECT regardless of similarity

When strictness = strict:
- Partial answers are INCORRECT — the predicted answer must cover all key claims in the reference
- Synonym or paraphrase of the core fact is fine, but omitting key parts is not

When strictness = lenient_paraphrase (used for low-complexity factual questions):
- The predicted answer is CORRECT if it conveys the same fact, even if phrased very differently
- Minor differences in units, date formats, or terminology are CORRECT
- Only mark INCORRECT if the answer is clearly about a different thing or contradicts the reference

When strictness = lenient_partial (used for high-complexity multi-hop questions):
- The predicted answer must correctly identify the core claim or central fact
- Minor omissions of supporting details are acceptable
- Mark INCORRECT only if the answer is wrong on the main point or contradicts the reference

Reference answer: {reference}
Predicted answer: {predicted}

Return JSON:
{{
  "correct": true or false,
  "explanation": "brief explanation of your judgment"
}}
